[phases.setup]
nixPkgs = ["python311", "nodejs_18", "python311Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn -w 1 --threads 8 -b 0.0.0.0:$PORT app:app"
